->Modificadores de acceso:
	Más usados:
		-Public
		-Private: misma clase
		-Protected
	Otros menos usados:
		-Internal
		-Protected internal
		-Private Protected
->Si empieza por I es una interfaz
->Propiedades lenguajes orientados a objetos
	-Herencia
	-Polimorfismo
	-Encapsulación
->Protected está disponible en los hijos y en el namespace
->El destructor ejecuta un código cuando el objeto es eliminado de memoria

->dotnet restore
->dotnet build
->dotnet test -a


->Realizar comprobaciones en capa de negocio: (controller)
